import { View, Text } from 'react-native';

export default function ParentHome() {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text style={{ fontSize: 18 }}>Parent Dashboard — Phase 1</Text>
    </View>
  );
}